# Upstash Redis via Vercel Marketplace (Storage → Redis)
# Vercel KV integrations inject these names:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Or native Upstash names:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
